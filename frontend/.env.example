# Leave unset in development: Vite proxies /api to the local API, which keeps
# the browser on one origin so session cookies behave as they will in
# production. Set only if you need to point at a deployed API.
VITE_API_BASE_URL=
